import 'package:flutter/material.dart';

class DynamicPage extends StatelessWidget {
  // const DynamicPage({required key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('首页'),
      ),
    );
  }
}
