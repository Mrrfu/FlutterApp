import 'package:flutter/material.dart';

class MinePage extends StatelessWidget {
  // const MinePage({required key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('个人中心'),
      ),
    );
  }
}
